{
  "name": "example-theming",
  "version": "0.0.1",
  "private": true,
  "description": "Palettes, brand logo, and a DI-based @AdminWidget() custom widget — no database, no Docker.",
  "license": "UNLICENSED",
  "type": "module",
  "scripts": {
    "build": "nest build",
    "start": "nest start",
    "start:dev": "nest start --watch",
    "start:prod": "node dist/main",
    "lint": "oxlint src",
    "check-types": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@nestjs-dash/core": "1.2.2",
    "@nestjs-dash/in-memory": "1.0.6",
    "@nestjs-dash/nestjs": "2.0.1",
    "@nestjs/common": "^12.0.1",
    "@nestjs/core": "^12.0.1",
    "@nestjs/platform-express": "^12.0.1",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.2"
  },
  "devDependencies": {
    "@nestjs/cli": "^12.0.0",
    "@nestjs/schematics": "^12.0.0",
    "@types/express": "^5.0.6",
    "@types/node": "^24.13.4",
    "oxlint": "^1.82.0",
    "typescript": "^6.0.2"
  }
}
